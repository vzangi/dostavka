script(id="orderTmpl" type="text/x-jquery-tmpl")
  tr(class="status-${status} table-${statuses[status-1].bgColorClass}")
    td 
      a(href="/admin/orders/${id}" target="_blank") ${id}
    td 
      span(title="${toDateString(createdAt)}" data-bs-toggle="tooltip" data-bs-placement="top" data-bs-html="true") ${coolDate}
    td ${statuses[status-1].name}
    td ${clientPhone}
    td 
      span(title="${city.name}" data-bs-toggle="tooltip" data-bs-placement="top" data-bs-html="true") ${address}
    td 
      a(href="/admin/stores/${storeId}" target="_blank" title='${store.address}<br>${store.phone}' data-bs-toggle="tooltip" data-bs-placement="top" data-bs-html="true") ${store.username}
    td ${summ} р.
    . 
      {{if driver}}
    td 
      a(href="/admin/drivers/${driverId}" target="_blank" title="${driver.phone}" data-bs-toggle="tooltip" data-bs-placement="top" data-bs-html="true") ${driver.username}
    . 
      {{else}}
    td Не назначен
    . 
      {{/if}}
    td

script(id="noOrdersTmpl" type="text/x-jquery-tmpl")
  tr(class="no-orders")
    td(colspan="10")
      span С текущим фильтром заказов не найдено